---
apiVersion: v1
kind: Secret
metadata:
  name: cluster-secrets
  namespace: flux-system
stringData:
  SECRET_DOMAIN: "{{ bootstrap_cloudflare_domain }}"
  SECRET_ACME_EMAIL: "{{ bootstrap_acme_email }}"
  SECRET_CLOUDFLARE_TUNNEL_ID: "{{ bootstrap_cloudflare_tunnel_id }}"
  SECRET_GHOST_MARIADB_USER: "{{ bootstrap_ghost_user }}"
  SECRET_GHOST_MARIADB_PASS: "{{ bootstrap_ghost_pw }}"
  SECRET_GHOST_MARIADB_DB: "{{ bootstrap_ghost_db }}"
